import 'package:flutter/material.dart';

class MVCServiceScreen extends StatelessWidget {
  const MVCServiceScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
